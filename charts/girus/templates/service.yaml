apiVersion: v1
kind: Service
metadata:
  name: {{ include "girus.fullname" . }}
  labels:
    {{- include "girus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "girus.selectorLabels" . | nindent 4 }}
